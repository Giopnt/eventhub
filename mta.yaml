_schema-version: 3.3.0
ID: eventhub
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: eventhub-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: eventhub-auth
      - name: eventhub-db
      - name: eventhub-destination

  - name: eventhub-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: eventhub-db

  - name: eventhub-app-deployer
    type: com.sap.application.content
    path: gen
    build-parameters:
      build-result: app/
      requires:
        - name: eventhubeventmgmt
          artifacts:
            - eventmgmt.zip
          target-path: app/
    requires:
      - name: eventhub-html5-repo-host
        parameters:
          content-target: true

  - name: eventhubeventmgmt
    type: html5
    path: app/eventmgmt
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []

  - name: eventhub-destinations
    type: com.sap.application.content
    requires:
      - name: eventhub-auth
        parameters:
          service-key:
            name: eventhub-auth-key
      - name: eventhub-html5-repo-host
        parameters:
          service-key:
            name: eventhub-html5-repo-host-key
      - name: eventhub-destination
        parameters:
          content-target: true
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: eventhub-html5-repository
              ServiceInstanceName: eventhub-html5-repo-host
              ServiceKeyName: eventhub-html5-repo-host-key
              sap.cloud.service: eventhub.service
            - Name: eventhub-auth
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: eventhub-auth
              ServiceKeyName: eventhub-auth-key
              sap.cloud.service: eventhub.service

resources:
  - name: eventhub-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: eventhub-${org}-${space}
        tenant-mode: dedicated
        oauth2-configuration:
          credential-types:
            - "binding-secret"
            - "x509"
        role-collections:
          - name: 'viewer (eventhub ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.viewer'
          - name: 'admin (eventhub ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.admin'
  - name: eventhub-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: eventhub-destination
    type: org.cloudfoundry.managed-service
    requires:
      - name: srv-api
    parameters:
      service: destination
      service-plan: lite
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: srv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true

  - name: eventhub-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host